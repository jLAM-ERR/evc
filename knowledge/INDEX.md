# Knowledge index

One line per entry: `- [title](category/file.md) — when to read it`.
Budget: this file stays under 200 lines (kb-lint enforces it). When it
approaches the budget, run the `distill` skill — never delete lines ad hoc.

<!--
Entry format (CONTRACT.md is authoritative):
- every entry is one md file in a category dir with frontmatter:
  id (12-hex content hash), status (candidate|approved|deprecated),
  source (gate|self-review|retro|human), date, refs, last_verified, topic
- every entry MUST have exactly one line in this index under its category
- keep the "when to read it" hook short — this whole file is a map, not a summary
-->

## Patterns

*(empty — populated by humans and gardening PRs that generalize solutions;
gate approvals route to Solutions first)*

## Conventions

*(empty)*

## Solutions

*(empty)*
- [vendored-lib-in-installable-plugins](solutions/20260727-vendored-lib-in-installable-plugins.md) — captured from human (approve)

## Anti-patterns

*(empty)*

## Glossary

*(empty)*
