# Area rules — example-area/

<!-- This file demonstrates NESTED per-area rules — the canonical,
     portable way to scope rules to a part of the tree (nearest AGENTS.md
     wins: touch-triggered in Kilo Code, cwd-walk in Codex, on-demand in
     Claude Code). Replace this file with rules for a real area (e.g.
     api/, frontend/, migrations/) or delete it. -->

Rules below apply ONLY to files under this directory and override the root
AGENTS.md where they conflict.

<!-- FILL: area-specific conventions. Examples:
     - "All handlers in this dir are async; never block the event loop."
     - "Migrations here are append-only; never edit an applied one." -->
